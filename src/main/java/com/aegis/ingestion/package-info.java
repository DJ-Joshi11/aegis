/**
 * Ingestion slice — owned by Person A.
 *
 * Accepts AI-agent decisions (real or mocked), validates and logs them,
 * and publishes them for the detection package to consume.
 *
 * See README.md in this folder for full scope, and the "Person A" prompt
 * in the project doc / docs/CLAUDE_PROMPTS.md to get a Claude session
 * building this package.
 */
package com.aegis.ingestion;
